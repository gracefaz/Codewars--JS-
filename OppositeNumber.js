// Task: Given an integer or a floating-point number, find its opposite.

function opposite(number) {
  return -number;
}

console.log(opposite(-27));
// Prints 27.
console.log(opposite(34));
// Prints -34.
console.log(opposite(0));
// Prints -0.
